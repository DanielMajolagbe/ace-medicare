# PostgreSQL — use Neon, Supabase, or Railway (free tiers available)
DATABASE_URL=postgresql://user:pass@host/db?sslmode=require

# Any long random string (32+ chars)
SESSION_SECRET=replace-me-with-a-long-random-secret

NODE_ENV=production
